import axios from 'axios';



const Configure = (props) => {

    return (
            <p>Need to update the rep list? <a onClick={props.salesConfigDisplayHandler}>Click here</a></p>
    )
}

export default Configure;